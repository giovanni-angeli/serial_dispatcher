# A sample implementation of an app using serial_asyncio to dispatch messages coming from serial ports 

there is a make.py script to be used to build, install and run tests.

please try:

    $ python3 make.py --help
    
and read the code inside make.py; it can be used as a guide on how to interact with the application in development.

