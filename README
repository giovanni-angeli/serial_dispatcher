
# A sample implementation of an app using serial_asyncio to dispatch messages coming from serial ports 

pre-requisites:

   $ sudo apt install virtualenv
   $ sudo apt install socat

to create a vitual env:

    $ python make.py make_venv
    
to run test in the vitual env:
    
    $ python make.py run_test
    
